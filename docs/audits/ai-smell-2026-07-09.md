# AI-codegen smell audit — 2026-07-09

## Summary
- Commit audited: `0689d9de848bb481dea4acef483095ce07118ae0` on branch `master`
- Scope: `src/` + `tools/` (production + CLI tooling), 84 hand-written `.cs` files; `tests/` for the mirror-test check
- Total findings: **3** (critical: 0 · major: 0 · minor: 1 · nit: 2)
- False-positive log applied: no — first run

This codebase reads as carefully human-reviewed: dense *why*-carrying comments (ADR references, measured rates, PR-review lineage), concrete domain naming, and behaviour-asserting tests. Eight of the ten checks found nothing; the two that hit found low-severity duplication, not fingerprint density.

## Findings

| Check | Severity | Location | Snippet | Suggested action |
|---|---|---|---|---|
| duplicated-helpers | minor | `src/FeedbackIntelligence.Api/Program.cs:249` | window parse: `to ?? …ToString("O")` → `TryNormalize` → `Parse` → "window must be positive…" | Extract `TryParseWindow(from, to, opts, out fromN, out toN, out error)`; the `/report` (249-260) and `/telemetry/corrections` (283-295) blocks are ~95% identical — a comment even says "Window parity with /report". |
| swallowed-errors | nit | `tools/FeedbackIntelligence.Ctl/Board.cs:165` | `catch { return null; }` | Narrow to the expected IO/access exception, or add a one-line intent comment (also raised by the .NET audit). |
| duplicated-helpers | nit | `tools/FeedbackIntelligence.StructuringEval/StructuringEvalRunner.cs:178` | `private static string Truncate(string s, int max) => s.Length <= max ? s : s[..max] + "…";` | Identical one-liner in sister file `MarkdownReport.cs:160`; fold into one shared util if that project is touched. Divergence risk near zero. |

## Findings by severity

### Critical
- None.

### Major
- None.

### Minor
- [src/FeedbackIntelligence.Api/Program.cs:249](../../src/FeedbackIntelligence.Api/Program.cs#L249) — the query-window parsing + validation logic (default-`to`, `TimestampNormalizer.TryNormalize`, `DateTimeOffset.Parse`, positive-and-bounded check) is duplicated ~95% between the `/report` and `/telemetry/corrections` endpoint handlers. The code itself carries a "Window parity with /report" comment — which is exactly the divergence risk this check exists for: a fix or bound change to one endpoint's window handling can silently miss the other. (duplicated-helpers)

### Nit
- [tools/FeedbackIntelligence.Ctl/Board.cs:165](../../tools/FeedbackIntelligence.Ctl/Board.cs#L165) — `catch { return null; }` with no log/comment; dev-only CLI board cosmetic, and the `null = unknown` return is documented on the method, hence nit. (swallowed-errors)
- [tools/FeedbackIntelligence.StructuringEval/StructuringEvalRunner.cs:178](../../tools/FeedbackIntelligence.StructuringEval/StructuringEvalRunner.cs#L178) — `Truncate` one-liner duplicated verbatim in `MarkdownReport.cs:160` within the same project. (duplicated-helpers)

## False-positive log

<!--
Append findings the auditor reviewed and dismissed. The next run reads this
section and skips any finding whose Check + Location appears here, unless the
line has changed since.
-->

Dismissed during this run (recorded so a future run doesn't re-litigate):

- `generic-names-in-domain-context` at `ReportService.cs:244/334/412` — reviewed 2026-07-09: `data`/`string data` is the payload spliced into the prompt's literal `{{data}}` placeholder; "data" IS the domain term and the name mirrors the template. Not a smell.
- `generic-names-in-domain-context` at `IngestService.cs:54`, `Program.cs:178` — reviewed 2026-07-09: `result` holds a typed `StructuringResult`; the type carries the meaning.
- `swallowed-errors` at `Ctl/ApiHost.cs:26/44/119`, `Shell.cs:69`, `Term.cs:53` — reviewed 2026-07-09: exception-as-absence; the `null`/`false` return IS the documented status contract (not-running / not-listening / API-unavailable / no-VT). `ApiHost.cs:119` additionally has a preceding intent comment. Legitimate counter-example.
- `defensive-checks-for-impossible-cases` (all sites) — reviewed 2026-07-09: every guard sits on a real trust boundary (config binding, HTTP body, LLM/JSON output, `JsonSerializer.Deserialize` → nullable) or a `string?` parameter. `<Nullable>enable</Nullable>` is on; no non-nullable param carries an unjustified null guard.
- `single-use-helpers` / `over-typed-primitives` / `stylistic-drift-within-file` / `paraphrase-comments` / `mirror-tests` — reviewed 2026-07-09: no qualifying findings (see notes below).

## Patterns observed but not in the checklist

- **Exception-as-absence contract (positive pattern).** The `Ctl` board probes deliberately return `null`/`false` from a `catch` to signal "not present / not reachable" — a documented status contract, not a swallow. Worth noting as a legitimate shape the swallowed-errors check must not over-flag; only the one comment-less `Board.cs:165` crosses the line.
- **Why-comment density (positive pattern).** Comments consistently reference ADRs (0004/0009/0015/0017/0018/0021/0022), measured numbers ("86% false-trend rate", "27/27 fenced"), and PR-review lineage. This is the inverse of the paraphrase-comment fingerprint and is a strong signal of human review.
- **`internal` for testability.** `TrendDirection` is `internal` (with `InternalsVisibleTo`) specifically for direct measurement in tests — a deliberate choice, not a single-use-helper smell.

**Bottom line:** no AI-codegen fingerprint density here. The one actionable item is the `/report` vs `/telemetry/corrections` window-parsing duplication (minor) — worth a small extraction next time that file is touched.

# .NET audit — 2026-07-03

## Summary
- Commit audited: `6e46eb3048878dc8e9129a3bebd921187d41f67a` on branch `main`
- Scope: repo root (8 .csproj / 1 .sln, 51 hand-written .cs, no Razor web surface — minimal APIs)
- Pre-flight: .NET codebase confirmed; proceeding with full audit
- Coverage: Phase 1 ran (build, vulnerable/outdated packages; `dotnet format` skipped — no EditorConfig policy in repo, style delegated to defaults); Phase 2 **2 of 5** subagents dispatched — groups with 0 candidates skipped (A DI ✓ dispatched · B EF Core — no EF Core in codebase, B3 cleared deterministically · C async — 0 candidates · D web — 0 candidates · E errors/resources ✓ dispatched)
- Total findings: **3** (critical: 0 · high: 3 · medium: 0 · low: 0) — all from dependency scanning; **0 code findings**

## Per-check tally

| Check | Candidates | Findings |
| --- | ---: | ---: |
| A1–A4 (DI & composition) | 19 | 0 |
| B1/B2/B4/B5 (EF Core) | n/a — no EF Core (raw Microsoft.Data.Sqlite by decision) | 0 |
| B3 missing-cancellationtoken | 2 (both line-wrap artifacts; `ct` on continuation line) | 0 |
| C1–C3 (async) | 0 | 0 |
| D1–D4 (web surface) | 0 | 0 |
| E1–E3 (errors/resources) | 22 | 0 |
| deps (Phase 1) | — | 3 |
| **Total** | | **3** |

## Static analysis
- `dotnet build -c Release`: **succeeded**, 0 errors, 7 warnings — all CS9057
  (OllamaSharp source-generator built against a newer Roslyn; benign, known
  since Phase 0).
- `dotnet list package --vulnerable --include-transitive`: **3 distinct High
  advisories** (findings below).
- `dotnet list package --outdated`: Microsoft.Extensions.* pinned `8.*` — this
  is the documented audience-stack decision (CLAUDE.md), not drift. xunit
  2.5.3 / Test.Sdk 17.8 are behind but only matter via the vulnerable
  transitive chain below.
- `dotnet format --verify-no-changes`: skipped (no repo style policy defined;
  running it would report defaults-vs-defaults noise).

## Findings by area

### Dependencies (Phase 1)
- [src/RetailFeedback.Api/RetailFeedback.Api.csproj:10](src/RetailFeedback.Api/RetailFeedback.Api.csproj#L10) [high] — vulnerable-transitive: `SQLitePCLRaw.lib.e_sqlite3 2.1.6` (GHSA-2m69-gcr7-jv3q, High) pulled transitively via `Microsoft.Data.Sqlite 8.*` into the **production API**.
- [tests/RetailFeedback.Llm.Tests/RetailFeedback.Llm.Tests.csproj:1](tests/RetailFeedback.Llm.Tests/RetailFeedback.Llm.Tests.csproj#L1) [high] — vulnerable-transitive: `System.Net.Http 4.3.0` (GHSA-7jgj-8wvc-jh57) and `System.Text.RegularExpressions 4.3.0` (GHSA-cmhx-cq75-c4mj) via the xunit 2.5.3 toolchain (test-only surface; same in Generator.Tests and Api.Tests).
- (counted once per advisory chain: SQLitePCLRaw also appears in Api.Tests via the project reference.)

### A — DI & composition
No findings. Judged: all 19 registration sites are composition-root; no scoped
services exist so captive-dependency is structurally impossible; `ReportService`
per-generation state is a local, not a field; `OllamaApiClient` is a keyed
singleton (one HttpClient for app lifetime); `LlmStructuringService._template`
lazy-cache race is benign-idempotent; all `IConfiguration` reads are
`GetSection().Bind/Get` at the composition root.

### B — EF Core & data access
Not applicable — the storage layer is raw `Microsoft.Data.Sqlite` (documented
decision). B3 (CancellationToken threading) verified deterministically: no
awaited async call on a request path drops the token.

### C — async/await & concurrency
0 candidates: no `.Result`/`.Wait()`/`GetAwaiter().GetResult()`, no
`async void`, no unobserved fire-and-forget in the codebase.

### D — web surface
0 candidates: no Razor surface, no `Redirect(`, no `[BindProperty]`. (Minimal
APIs: the overposting/GET-mutation shapes are covered by the DTO contracts and
endpoint review in docs/prs/0002–0004.)

### E — error handling & resources
No findings across 22 catch/disposable sites. Notable verifications: the
/health exception filter `when (ex is not OperationCanceledException ||
!ct.IsCancellationRequested)` was traced through client-cancel /
internal-timeout / connection-refused and behaves correctly in each; every
broad catch either rethrows cancellation first or carries a documented
CLAUDE.md invariant ("LLM down ≠ feedback lost", "snapshot persistence never
fails a live report"); all owned disposables are `using`-scoped.

## Recommended next steps
1. [high] `fix/dotnet-deps` — one commit: add a direct
   `SQLitePCLRaw.bundle_e_sqlite3` (≥ 2.1.10) reference to RetailFeedback.Api
   to lift the vulnerable transitive; re-run the vulnerable scan.
2. [high, test-only] same branch, one commit: bump the xunit toolchain (or pin
   `System.Net.Http` 4.3.4 / `System.Text.RegularExpressions` 4.3.1 directly
   in the three test projects); re-run tests.

Work in parallel across branches; the five scopes are non-overlapping by
construction — though here a single small deps branch covers everything found.

## What's verifiable vs editorial

| Claim | Source of truth | Verifiable? |
| --- | --- | --- |
| Is this a .NET codebase? | .csproj/.sln + .cs files | ✅ Yes (pre-flight) |
| Does pattern X appear at file:line Y? | The source file | ✅ Yes |
| Is pattern X a bug *here*? | Human judgement | 🟡 Heuristic (counter-examples applied) |
| Vulnerable package versions | `dotnet list package --vulnerable` + GHSA | ✅ Yes |
| Severity of a finding | Default + auditor adjustment | 🟡 Heuristic |
